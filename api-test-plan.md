                                    # 智能排课系统接口测试计划

## 1. 测试目标

对系统中所有接口进行全面功能测试，验证其是否符合设计规范和业务需求。测试内容包括但不限于接口的请求参数验证、响应数据格式正确性、错误处理机制、边界条件处理等。同时，模拟前端与后端的交互场景，测试接口在实际使用环境中的表现。

## 2. 测试范围

本次测试覆盖系统中的所有REST API接口，包括：

### 2.1 认证模块 (AuthController)
- POST /auth/login - 用户登录
- GET /auth/info - 获取当前用户信息
- POST /auth/logout - 退出登录

### 2.2 课表管理模块 (TimetableController)
- POST /timetable/generate - 生成课表
- POST /timetable/{id}/publish - 发布课表
- POST /timetable/{id}/archive - 归档课表
- DELETE /timetable/{id} - 删除课表
- GET /timetable/page - 分页查询课表
- GET /timetable/{id} - 根据ID查询课表
- GET /timetable/{id}/details - 查询课表明细
- GET /timetable/{id}/class/{classId} - 查询班级课表
- GET /timetable/{id}/teacher/{teacherId} - 查询教师课表
- GET /timetable/{id}/classroom/{classroomId} - 查询教室课表
- GET /timetable/latest - 获取最新课表
- GET /timetable/{id}/conflicts - 查询冲突记录

### 2.3 排课管理模块 (ScheduleController)
- POST /schedule/execute - 执行排课
- POST /schedule/async - 异步排课
- GET /schedule/algorithms - 获取算法列表

### 2.4 教学任务模块 (TeachingTaskController)
- GET /task/page - 分页查询教学任务
- GET /task/{id} - 根据ID查询教学任务
- POST /task - 新增教学任务
- PUT /task - 更新教学任务
- DELETE /task/{id} - 删除教学任务

### 2.5 调课管理模块 (AdjustmentController)
- POST /adjustment/check - 检测调课冲突
- POST /adjustment/execute - 执行调课
- POST /adjustment/swap - 交换两门课程

### 2.6 统计分析模块 (StatisticsController)
- GET /statistics/classroom-utilization/{timetableId} - 教室利用率统计
- GET /statistics/teacher-workload/{timetableId} - 教师工作量统计
- GET /statistics/conflict-report/{timetableId} - 冲突报告
- GET /statistics/total-hours/{timetableId} - 总排课学时
- GET /statistics/course-count/{timetableId} - 课程数量统计

## 3. 测试方法

### 3.1 单元测试
- 使用Postman或curl等工具直接调用API接口
- 验证接口的请求参数验证、响应数据格式正确性、错误处理机制等

### 3.2 集成测试
- 模拟前端与后端的交互场景
- 测试接口在实际使用环境中的表现，包括数据传输的完整性、交互流程的顺畅性、以及异常情况下的用户体验

### 3.3 边界测试
- 测试接口的边界条件，如参数为空、参数过长、参数格式错误等
- 测试接口在大数据量情况下的表现

## 4. 测试用例设计

### 4.1 认证模块测试用例

#### 4.1.1 POST /auth/login

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常登录 | `{"username": "admin", "password": "123456"}` | 成功登录，返回token和用户信息 |
| 用户名不存在 | `{"username": "nonexistent", "password": "123456"}` | 登录失败，返回错误信息 |
| 密码错误 | `{"username": "admin", "password": "wrongpassword"}` | 登录失败，返回错误信息 |
| 用户名为空 | `{"username": "", "password": "123456"}` | 参数验证失败，返回错误信息 |
| 密码为空 | `{"username": "admin", "password": ""}` | 参数验证失败，返回错误信息 |

#### 4.1.2 GET /auth/info

| 测试场景 | 请求头 | 预期结果 |
|---------|-------|--------|
| 已登录状态 | `Authorization: Bearer {token}` | 返回当前用户信息 |
| 未登录状态 | 无Authorization头 | 返回401未授权错误 |
| 无效token | `Authorization: Bearer invalidtoken` | 返回401未授权错误 |

#### 4.1.3 POST /auth/logout

| 测试场景 | 请求头 | 预期结果 |
|---------|-------|--------|
| 已登录状态 | `Authorization: Bearer {token}` | 成功退出登录 |
| 未登录状态 | 无Authorization头 | 成功退出登录（幂等操作） |

### 4.2 课表管理模块测试用例

#### 4.2.1 POST /timetable/generate

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常生成 | 完整的排课请求参数 | 成功生成课表，返回课表信息 |
| 参数不完整 | 缺少必要参数 | 参数验证失败，返回错误信息 |
| 权限不足 | 非管理员用户 | 返回403权限不足错误 |

#### 4.2.2 POST /timetable/{id}/publish

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常发布 | 有效的课表ID | 成功发布课表，返回更新后的课表信息 |
| 无效ID | 不存在的课表ID | 返回404资源不存在错误 |
| 权限不足 | 非管理员用户 | 返回403权限不足错误 |

#### 4.2.3 POST /timetable/{id}/archive

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常归档 | 有效的课表ID | 成功归档课表，返回更新后的课表信息 |
| 无效ID | 不存在的课表ID | 返回404资源不存在错误 |
| 权限不足 | 非管理员用户 | 返回403权限不足错误 |

#### 4.2.4 DELETE /timetable/{id}

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常删除 | 有效的课表ID | 成功删除课表 |
| 无效ID | 不存在的课表ID | 返回404资源不存在错误 |
| 权限不足 | 非管理员用户 | 返回403权限不足错误 |

#### 4.2.5 GET /timetable/page

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常查询 | 分页参数 | 成功返回分页课表列表 |
| 带学期过滤 | 分页参数 + semester | 成功返回指定学期的课表列表 |
| 边界分页 | page=1, size=1000 | 成功返回分页课表列表 |

#### 4.2.6 GET /timetable/{id}

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常查询 | 有效的课表ID | 成功返回课表信息 |
| 无效ID | 不存在的课表ID | 返回404资源不存在错误 |

#### 4.2.7 GET /timetable/{id}/details

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常查询 | 有效的课表ID | 成功返回课表明细列表 |
| 无效ID | 不存在的课表ID | 返回404资源不存在错误 |

#### 4.2.8 GET /timetable/{id}/class/{classId}

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常查询 | 有效的课表ID和班级ID | 成功返回班级课表 |
| 无效课表ID | 不存在的课表ID | 返回404资源不存在错误 |
| 无效班级ID | 不存在的班级ID | 返回空列表 |

#### 4.2.9 GET /timetable/{id}/teacher/{teacherId}

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常查询 | 有效的课表ID和教师ID | 成功返回教师课表 |
| 无效课表ID | 不存在的课表ID | 返回404资源不存在错误 |
| 无效教师ID | 不存在的教师ID | 返回空列表 |

#### 4.2.10 GET /timetable/{id}/classroom/{classroomId}

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常查询 | 有效的课表ID和教室ID | 成功返回教室课表 |
| 无效课表ID | 不存在的课表ID | 返回404资源不存在错误 |
| 无效教室ID | 不存在的教室ID | 返回空列表 |

#### 4.2.11 GET /timetable/latest

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常查询 | 有效的学期参数 | 成功返回最新课表 |
| 学期不存在 | 不存在的学期 | 返回空或错误信息 |
| 无学期参数 | 缺少学期参数 | 参数验证失败，返回错误信息 |

#### 4.2.12 GET /timetable/{id}/conflicts

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常查询 | 有效的课表ID | 成功返回冲突记录列表 |
| 无效ID | 不存在的课表ID | 返回404资源不存在错误 |

### 4.3 排课管理模块测试用例

#### 4.3.1 POST /schedule/execute

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常执行 | 完整的排课请求参数 | 成功执行排课，返回排课结果 |
| 参数不完整 | 缺少必要参数 | 参数验证失败，返回错误信息 |
| 权限不足 | 非管理员用户 | 返回403权限不足错误 |

#### 4.3.2 POST /schedule/async

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常提交 | 完整的排课请求参数 | 成功提交排课任务，返回任务ID |
| 参数不完整 | 缺少必要参数 | 参数验证失败，返回错误信息 |
| 权限不足 | 非管理员用户 | 返回403权限不足错误 |

#### 4.3.3 GET /schedule/algorithms

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常查询 | 无参数 | 成功返回算法列表 |

### 4.4 教学任务模块测试用例

#### 4.4.1 GET /task/page

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常查询 | 分页参数 | 成功返回分页教学任务列表 |
| 带学期过滤 | 分页参数 + semester | 成功返回指定学期的教学任务列表 |
| 边界分页 | page=1, size=1000 | 成功返回分页教学任务列表 |

#### 4.4.2 GET /task/{id}

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常查询 | 有效的任务ID | 成功返回教学任务信息 |
| 无效ID | 不存在的任务ID | 返回404资源不存在错误 |

#### 4.4.3 POST /task

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常新增 | 完整的教学任务参数 | 成功新增教学任务 |
| 参数不完整 | 缺少必要参数 | 参数验证失败，返回错误信息 |
| 权限不足 | 非管理员用户 | 返回403权限不足错误 |

#### 4.4.4 PUT /task

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常更新 | 完整的教学任务参数 | 成功更新教学任务 |
| 无效ID | 不存在的任务ID | 返回404资源不存在错误 |
| 权限不足 | 非管理员用户 | 返回403权限不足错误 |

#### 4.4.5 DELETE /task/{id}

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常删除 | 有效的任务ID | 成功删除教学任务 |
| 无效ID | 不存在的任务ID | 返回404资源不存在错误 |
| 权限不足 | 非管理员用户 | 返回403权限不足错误 |

### 4.5 调课管理模块测试用例

#### 4.5.1 POST /adjustment/check

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常检测 | 完整的调课请求参数 | 成功检测调课冲突，返回检测结果 |
| 参数不完整 | 缺少必要参数 | 参数验证失败，返回错误信息 |

#### 4.5.2 POST /adjustment/execute

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常执行 | 完整的调课请求参数 | 成功执行调课，返回调课结果 |
| 参数不完整 | 缺少必要参数 | 参数验证失败，返回错误信息 |
| 权限不足 | 非管理员/教师用户 | 返回403权限不足错误 |
| 存在冲突 | 调课请求存在冲突 | 返回冲突信息，调课失败 |

#### 4.5.3 POST /adjustment/swap

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常交换 | 有效的课表ID和两个课程ID | 成功交换课程，返回交换结果 |
| 无效课表ID | 不存在的课表ID | 返回404资源不存在错误 |
| 无效课程ID | 不存在的课程ID | 返回404资源不存在错误 |
| 权限不足 | 非管理员/教师用户 | 返回403权限不足错误 |
| 存在冲突 | 交换后存在冲突 | 返回冲突信息，交换失败 |

### 4.6 统计分析模块测试用例

#### 4.6.1 GET /statistics/classroom-utilization/{timetableId}

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常查询 | 有效的课表ID | 成功返回教室利用率统计数据 |
| 无效ID | 不存在的课表ID | 返回404资源不存在错误 |

#### 4.6.2 GET /statistics/teacher-workload/{timetableId}

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常查询 | 有效的课表ID | 成功返回教师工作量统计数据 |
| 无效ID | 不存在的课表ID | 返回404资源不存在错误 |

#### 4.6.3 GET /statistics/conflict-report/{timetableId}

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常查询 | 有效的课表ID | 成功返回冲突报告 |
| 无效ID | 不存在的课表ID | 返回404资源不存在错误 |

#### 4.6.4 GET /statistics/total-hours/{timetableId}

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常查询 | 有效的课表ID | 成功返回总排课学时 |
| 无效ID | 不存在的课表ID | 返回404资源不存在错误 |

#### 4.6.5 GET /statistics/course-count/{timetableId}

| 测试场景 | 请求参数 | 预期结果 |
|---------|---------|--------|
| 正常查询 | 有效的课表ID | 成功返回课程数量统计 |
| 无效ID | 不存在的课表ID | 返回404资源不存在错误 |

## 5. 测试环境

### 5.1 后端环境
- Spring Boot 3.2.1
- Java 17+
- MySQL 8.0
- Redis 7.0+

### 5.2 前端环境
- Vue 3
- Vant 4
- Vite
- Node.js 16+

### 5.3 测试工具
- Postman
- curl
- Chrome DevTools

## 6. 测试执行计划

### 6.1 准备阶段
1. 搭建测试环境
2. 准备测试数据
3. 配置测试工具

### 6.2 执行阶段
1. 按照测试用例执行接口测试
2. 记录测试结果
3. 分析测试数据

### 6.3 报告阶段
1. 整理测试结果
2. 对发现的问题进行分类和优先级排序
3. 提供详细的复现步骤和改进建议

## 7. 测试标准

### 7.1 成功标准
- 所有接口的正常场景测试通过
- 所有接口的错误处理机制正常
- 所有接口的边界条件处理正确
- 前端与后端的交互流程顺畅

### 7.2 失败标准
- 接口的正常场景测试失败
- 接口的错误处理机制异常
- 接口的边界条件处理错误
- 前端与后端的交互流程不顺畅

## 8. 风险评估

### 8.1 潜在风险
- 测试数据准备不充分，导致测试结果不准确
- 测试环境配置不当，导致测试结果异常
- 接口文档与实际实现不一致，导致测试用例设计错误

### 8.2 风险缓解措施
- 充分准备测试数据，确保测试数据的多样性和完整性
- 严格按照环境配置要求搭建测试环境
- 在测试前仔细核对接口文档与实际实现，确保测试用例设计的准确性

## 9. 测试团队

| 角色 | 职责 |
|-----|------|
| 测试负责人 | 负责测试计划的制定和执行，协调测试资源，汇总测试结果 |
| 测试执行人员 | 负责按照测试用例执行测试，记录测试结果，提交测试问题 |
| 开发人员 | 负责修复测试过程中发现的问题，提供技术支持 |

## 10. 测试时间计划

| 阶段 | 时间安排 |
|-----|--------|
| 测试计划制定 | 1天 |
| 测试环境搭建 | 1天 |
| 测试数据准备 | 1天 |
| 测试执行 | 3天 |
| 测试结果分析 | 1天 |
| 测试报告编写 | 1天 |

## 11. 附录

### 11.1 测试数据准备

#### 11.1.1 认证测试数据
- 管理员账号：admin / 123456
- 教师账号：teacher001 / 123456
- 学生账号：student001 / 123456

#### 11.1.2 教学任务测试数据
- 学期：2024-1
- 课程：程序设计基础、数据结构、操作系统等
- 教师：张三、李四、王五等
- 班级：计算机科学2021级1班、计算机科学2021级2班等

#### 11.1.3 课表测试数据
- 课表ID：1
- 学期：2024-1
- 生成方式：贪心算法

### 11.2 测试工具配置

#### 11.2.1 Postman配置
- 基础URL：http://localhost:8081
- 认证方式：Bearer Token
- 超时时间：30秒

#### 11.2.2 curl配置
- 基础命令：`curl -X GET http://localhost:8081/api/{endpoint}`
- 认证命令：`curl -X GET http://localhost:8081/api/{endpoint} -H "Authorization: Bearer {token}"`

### 11.3 接口文档

接口文档可通过Swagger UI访问：http://localhost:8081/doc.html
