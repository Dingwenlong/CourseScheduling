# 智能排课系统序列图解释 

## 1\. 序列图概述

本序列图展示了智能排课系统中各组件之间的交互流程，包括用户、前端应用、后端API、业务服务、数据库和缓存系统。图中涵盖了系统的核心业务流程，包括用户认证、课表管理、调课操作和统计分析等功能。

## 2\. 关键参与者

* **用户**：系统的最终使用者，通过前端应用与系统交互
* **前端应用**：基于Vue 3和Vant 4开发的Web应用，负责用户界面和交互逻辑
* **后端API**：基于Spring Boot开发的RESTful API，处理前端请求并返回响应
* **业务服务**：实现系统核心业务逻辑的服务层组件
* **数据库**：MySQL 8.0数据库，存储系统数据
* **缓存**：Redis 7.0+缓存，存储token和临时数据

## 3\. 核心交互流程

### 3.1 用户登录流程

1. 用户在前端应用输入用户名和密码
2. 前端应用验证输入合法性
3. 前端应用向后端API发送登录请求
4. 后端API调用UserService.login()方法处理登录逻辑
5. UserService查询数据库获取用户信息
6. 如果用户存在且密码正确，生成并存储token到Redis，返回登录响应
7. 前端应用存储token并显示登录成功
8. 如果用户不存在或密码错误，返回认证失败响应

### 3.2 获取用户信息流程

1. 用户访问需要认证的页面
2. 前端应用携带token向后端API发送获取用户信息请求
3. 后端API验证token有效性
4. 如果token有效，查询数据库获取用户信息并返回
5. 如果token无效或过期，返回未授权响应，前端应用跳转到登录页

### 3.3 生成课表流程

1. 用户在前端应用填写排课参数
2. 前端应用向后端API发送生成课表请求
3. 后端API调用TimetableService.generate()方法处理排课逻辑
4. TimetableService查询数据库获取教学任务
5. 执行排课算法生成课表
6. 如果排课成功，保存课表到数据库并返回成功响应
7. 如果排课失败，返回错误响应

### 3.4 发布课表流程

1. 用户在前端应用点击发布课表按钮
2. 前端应用向后端API发送发布课表请求
3. 后端API调用TimetableService.publish()方法处理发布逻辑
4. TimetableService更新课表状态到数据库
5. 返回更新后的课表信息

### 3.5 调课流程

1. 用户在前端应用填写调课信息
2. 前端应用向后端API发送调课冲突检查请求
3. 后端API调用AdjustmentService.check()方法检查冲突
4. AdjustmentService查询数据库检查调课冲突
5. 如果无冲突，返回检查通过响应，前端应用提示用户确认
6. 用户确认后，前端应用发送执行调课请求
7. 后端API调用AdjustmentService.execute()方法执行调课
8. AdjustmentService更新数据库中的课程信息
9. 返回调课成功响应
10. 如果有冲突，返回冲突信息，调课失败

### 3.6 统计分析流程

1. 用户在前端应用选择统计类型
2. 前端应用向后端API发送统计分析请求
3. 后端API调用StatisticsService.calculate()方法处理统计逻辑
4. StatisticsService查询数据库获取原始数据
5. 计算统计结果并返回
6. 前端应用显示统计结果

## 4\. 错误处理机制

序列图中包含了以下错误处理场景：

1. **认证错误**：用户不存在或密码错误时，返回401未授权错误
2. **Token错误**：token无效或过期时，返回401未授权错误，前端应用跳转到登录页
3. **排课错误**：排课失败时，返回500内部服务器错误
4. **调课冲突**：调课存在冲突时，返回冲突信息，调课失败

## 5\. 技术实现细节

### 5.1 前端实现

* 使用Axios发送HTTP请求
* 实现请求拦截器，自动添加token到请求头
* 实现响应拦截器，统一处理错误响应
* 使用localStorage存储token

### 5.2 后端实现

* 使用Spring Boot 3.2.1开发RESTful API
* 使用Spring Security和JWT实现认证机制
* 使用MyBatis Plus操作数据库
* 使用Redis存储token和临时数据

### 5.3 数据传输

* 使用JSON格式传输数据
* 统一响应格式：{"code": 200, "message": "成功", "data": {...}}
* 错误响应格式：{"code": 错误码, "message": "错误信息"}

## 6\. 序列图使用说明

本序列图使用Mermaid语法编写，可通过以下方式查看：

1. 使用支持Mermaid的Markdown编辑器（如Visual Studio Code的Mermaid插件）
2. 在线Mermaid编辑器（如https://mermaid.live/）
3. 转换为PNG或SVG格式的图像文件

## 7\. 总结

本序列图全面展示了智能排课系统的核心交互流程，包括用户认证、课表管理、调课操作和统计分析等功能。图中详细描述了各组件之间的消息交换、时序约束和决策点，准确反映了系统的运行工作流，包括错误处理路径和替代场景。

通过本序列图，可以清晰地了解系统的架构设计和工作原理，为系统的开发、测试和维护提供参考。

